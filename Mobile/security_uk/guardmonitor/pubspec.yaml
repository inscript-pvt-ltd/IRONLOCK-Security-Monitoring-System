name: guardmonitor
description: "IronLock Guard Monitor — lone-worker safety app for security guards: shift check-in/out, live GPS tracking, welfare (wakefulness) checks, photo verification, and offline sync."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.0

# Phase 7: build the SQLCipher (encrypted) flavour of sqlite3 instead of plain
# SQLite, so the offline-sync queue (lib/data/offline_queue_db.dart) is encrypted
# at rest. sqlite3 3.x selects the engine through this build-hook user-define
# (requires `flutter config --enable-native-assets`). A prebuilt SQLCipher
# community binary is fetched by the hook — nothing is compiled from source here.
hooks:
  user_defines:
    sqlite3:
      source: sqlcipher

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.1
  google_fonts: ^8.1.0

  # Networking
  dio: ^5.7.0
  flutter_secure_storage: ^9.2.2
  connectivity_plus: ^6.1.0

  # Permissions
  permission_handler: ^11.3.1

  # Location — geolocator re-exports AndroidSettings/AppleSettings/
  # ForegroundNotificationConfig used for background patrol tracking.
  geolocator: ^13.0.0

  # Device battery (real reading for the status strip + GPS pings)
  battery_plus: ^6.2.0

  # Camera
  camera: ^0.11.0

  # Cryptography (HMAC-SHA256 photo signing)
  crypto: ^3.0.3

  # Phase 7 offline sync — encrypted on-device queue (Drift over SQLCipher) +
  # SNTP anchor for photo capture-time proof.
  drift: ^2.34.0
  # The SQLCipher engine is selected via the build-hook `user_defines` below
  # (sqlite3 3.x dropped the old `sqlcipher_flutter_libs` / `open.overrideFor`
  # mechanism in favour of build hooks). `sqlite3` is a direct dep so we can call
  # `PRAGMA key` / `PRAGMA cipher_version` on the raw handle.
  sqlite3: ^3.1.5
  path_provider: ^2.1.0
  path: ^1.9.0
  ntp: ^2.0.0

  flutter_local_notifications: ^22.0.1
  timezone: ^0.11.0
  firebase_core: ^4.11.0
  firebase_messaging: ^16.4.1
  app_links: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

  # Drift codegen (offline queue tables). Kept in lockstep with the drift runtime.
  drift_dev: ^2.34.0
  build_runner: ^2.4.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/
    - assets/legal/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# App launcher icon (IronLock gold phoenix on brand navy #07111F). Generated
# from assets/icons/* via: dart run flutter_launcher_icons
# - app_icon.png: square, dark-navy bg, no alpha (iOS-safe; iOS forbids alpha).
# - app_icon_foreground.png: transparent phoenix for the Android adaptive icon,
#   sized small so the OS circular/squircle mask never clips it.
flutter_launcher_icons:
  image_path: "assets/icons/app_icon.png"
  remove_alpha_ios: true
  background_color_ios: "#07111F"
  android: true
  ios: true
  min_sdk_android: 21
  adaptive_icon_background: "#07111F"
  adaptive_icon_foreground: "assets/icons/app_icon_foreground.png"
